/**
 * focus shared capability group.
 *
 * Add one single-capability helper per file (never a broad "verify component"),
 * then export it from this index. A helper is promoted to a shared group only
 * when two or more components share the exact observable promise.
 */
export {}
