/**
 * disclosure shared capability group.
 *
 * The Accordion disclosure helpers are the first seed of this group and are
 * exported from the package root (`accordion/manifest.js`,
 * `accordion/verify*`). This directory hosts NEW disclosure helpers (banner,
 * header, sidenav) that add a capability beyond the Accordion seed; promote one
 * here only when two or more components share the exact observable promise
 * (Constitution XIV, FR-013).
 *
 * Note: the Accordion seed is intentionally not re-exported from here to avoid
 * ambiguous re-exports at the package root; import it from the package root.
 */
export {}
