# Critique Report: Fix Modal Backdrop

**Date**: 2026-09-18
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Verdict**: ✅ PROCEED
**Prior critique**: [critique-20260918-110115.md](./critique-20260918-110115.md) (⚠️ PROCEED WITH UPDATES — remediations verified in this re-run)

---

## Executive Summary

Re-critique after remediation: prior **must-address** items (**E1**, **E2**) and the open product question (**P4**) are **resolved** in `spec.md`, `plan.md`, `research.md`, `data-model.md`, `contracts/modal-open-presentation.md`, and `quickstart.md`. The open class list is locked (`.pathable-modal-wrapper.is-visible` → `.pathable-modal-overlay` → `.pathable-modal` + `.usa-modal`), styles must ship explicit PathAble open SCSS with a PathAble-only Storybook proof, React must emit the dual-class shell, and backdrop dismiss is answered via `closeOnBackdropClick` default `false`. Constitution I/IV/V/X/XII/XIII/XIV/XVI gates remain satisfied. No new blockers; two light recommendations remain for tasking polish. Ready for `/speckit.tasks`.

---

## Prior Must-Address Remediation Status

| Prior ID | Topic | Status | Evidence |
|----------|-------|--------|----------|
| E1 / X1 | Locked open class list + explicit PathAble `.is-visible` SCSS | **Resolved** | Clarifications Q4; FR-001/FR-006; contract table; research §2; data-model §1; plan Implementation Sequence steps 1–2 |
| E2 | Dual-class policy: styles PathAble-only proof; React PathAble + USWDS shell | **Resolved** | FR-005; contract “Dual-class policy”; research §2–3; Constitution Check open-contract proof |
| P4 / X2 | Backdrop click dismiss undecided | **Resolved** | Clarifications Q5; FR-007/FR-010; US-2 scenarios 4–5; contract React API table; research §4 |

Prior 💡 items (plan sequence, compiled-CSS grep, shell open/absent assertions, changelog/migration, docs copy, SC done-when/rollback, ARIA/`...rest` on dialog) are reflected in current artifacts and are **not re-opened**.

---

## Product Lens Findings 🎯

### Problem Validation
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P1 | — | No new issue. Problem remains clear (#246); scope narrow; SC-009 supplies done-when + rollback. | None — proceed. |

### User Value Assessment
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P2 | — | No new issue. P1 stories + SC-001 consumer outcome (“blocking message reads as visually modal”) are adequate. | None — proceed. |

### Alternative Approaches
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P3 | — | No new issue. Approach and migration notes for ad-hoc overlay consumers are documented. | None — proceed. |

### Edge Cases & UX
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P4 | — | **Prior P4 resolved.** `closeOnBackdropClick` default `false` with opt-in overlay dismiss is specified end-to-end. | None — do not re-flag. |

### Success Measurement
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P5 | — | No new issue. SC-002 per-package visual fixtures + SC-009 rollback are clear. | None — proceed. |

---

## Engineering Lens Findings 🔬

### Architecture Soundness
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E1 | — | **Prior E1 resolved.** Required open list + explicit PathAble open SCSS are locked; plan sequences SCSS → dist grep → styles stories → React. | None — do not re-flag. |
| E2 | — | **Prior E2 resolved.** Dual-class policy is precise (styles PathAble-only proof; React dual-class emission). | None — do not re-flag. |
| E3 | 💡 | FR-001 still hedges “or the documented PathAble open modifier” while contract/plan/research lock `.pathable-modal-wrapper.is-visible`. Low risk of implementer drift if tasks copy FR hedge. | In tasks (or a one-line FR-001 tidy if editing), prefer the locked `.is-visible` selector as the acceptance name; keep “or documented modifier” only if a second modifier is intentionally allowed. |

### Failure Mode Analysis
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E4 | — | No new issue. `@extend` compound failure mode addressed via explicit PathAble rules + dist grep. | None — proceed. |

### Security & Privacy
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E5 | — | No new issue. Presentational overlay; a11y regressions remain the main risk (already gated). | None — proceed. |

### Performance & Scalability
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E6 | — | No new issue. Negligible portal cost; inherited z-index documented as “no change by default.” | None — proceed. |

### Testing Strategy
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E7 | 💡 | FR-010 requires `closeOnBackdropClick` true/false behavior to be **verifiable**, but contract Story fixtures and research §6 mark coverage as **optional**. Default-false and opt-in-true could ship without automated proof if tasks only implement the prop. | In `/speckit.tasks`, require at least one React interaction/unit check for default (`false`: overlay click does not call `onClose`) and one for `true` (overlay → `onClose`; dialog content does not). Prefer accessible outcomes over CSS-only asserts. |

### Operational Readiness
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E8 | — | No new issue. Changelog/migration for portal shell + stacked-dimmer removal is specified. | None — proceed. |

### Dependencies & Integration
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E9 | — | No new issue. Docs/BRAND_RULES updates and dialog attribute placement are in plan/contract. | None — proceed. |

---

## Cross-Lens Insights 🔗

Items where both product and engineering perspectives converge:

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|---------------|-------------------|------------|
| X1 | Prior X1 (class-contract lock) — **resolved** | Styles consumers get working PathAble-only open | No silent dual-class-only “fix” | Keep locked contract through tasks/implement |
| X2 | Prior X2 (backdrop dismiss) — **resolved** | Predictable default for blocking dialogs; opt-in click-outside | Clear React-only prop boundary | Task coverage for both defaults (see E7) |
| X3 | Prior X3 (portal DOM migration) — **resolved** | Consumers warned to drop ad-hoc overlays | Changelog for DOM-query consumers | Carry into release notes during implement |

No new cross-lens blockers.

---

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendations | 2 |
| 🤔 Questions | 0 |
| Product findings (new open) | 0 |
| Engineering findings (new open) | 2 |
| Cross-lens findings (new open) | 0 |

---

## Consolidated Findings Table

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| E3 | Engineering | 💡 | Architecture | FR-001 “or open modifier” hedge vs locked `.is-visible` | Prefer `.is-visible` as acceptance name in tasks / optional FR tidy |
| E7 | Engineering | 💡 | Testing | Backdrop-click prop verifiable in FR-010 but story coverage optional | Require automated true/false coverage in tasks |

*(Prior 🎯 E1/E2 and 🤔 P4 are resolved — not listed as open findings.)*

---

## Recommended Actions

### 🎯 Must-Address (Before Proceeding)
*None.*

### 💡 Recommendations (Strongly Suggested)
1. **E7**: When generating tasks, include React interaction/unit coverage for `closeOnBackdropClick` default `false` and `true` (overlay vs dialog-content clicks).
2. **E3**: In tasks (or a tiny FR-001 wording pass), treat `.pathable-modal-wrapper.is-visible` as the canonical open visibility class unless a second modifier is intentionally documented.

### 🤔 Questions (Need Stakeholder Input)
*None.*

---

## Proposed Edits (propose only — not applied)

Optional polish only; **not required** for ✅ PROCEED:

### A. `contracts/modal-open-presentation.md` — Story fixtures

Change optional backdrop-click coverage to required for React:

```markdown
Interaction (React): existing Escape / Tab / close plays remain green. Plays MUST
assert wrapper/overlay (or dialog contained in overlay) when open, and assert those
shell nodes are absent when closed. MUST cover `closeOnBackdropClick` default
(`false`: overlay click does not call `onClose`) and `true` (overlay → `onClose`;
dialog content clicks do not).
```

### B. `spec.md` FR-001 (optional consistency)

Replace the hedge with the locked selector:

> … wrapper `.pathable-modal-wrapper.is-visible` …

(omit “or the documented PathAble open modifier” unless a second modifier is planned)

---

## Verdict

✅ **PROCEED** — Prior must-address items and the backdrop-dismiss question are resolved. Remaining items are non-blocking tasking polish (backdrop-click test coverage; `.is-visible` wording consistency). Run `/speckit.tasks` to break the plan into actionable tasks.

**Remediation**: Optional proposed edits above are offered for parent/user approval; **not applied** in this critique run.

---

**Severity Legend**:
- 🎯 **Must-Address**: Blocks proceeding to implementation
- 💡 **Recommendation**: Strongly suggested improvement
- 🤔 **Question**: Needs stakeholder input to resolve

---

*Generated by `/speckit.critique` — Dual-lens strategic and technical review for spec-driven development.*
