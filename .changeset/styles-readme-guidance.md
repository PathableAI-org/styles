---
'@pathableai/styles': patch
---

Emit default tokens with zero selector specificity so application-owned `:root` declarations win regardless of stylesheet order. Also clarify the packaged README's React and CSS-only guidance.
