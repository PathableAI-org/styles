---
'@pathableai/react': minor
---

Add accessible-by-default FormGroup composition that generates stable field IDs and associates supported labels, hints, and errors while preserving explicit consumer ARIA attributes.
