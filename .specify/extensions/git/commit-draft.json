1. packages/react/src/components/Text/Text.tsx — fixed `TextProps` type so `as` is included in the exported type rather than an ad-hoc inner param.
2. packages/react/src/stories/components/Text/Text.stories.tsx — fixed the Composition story to use a `<span>` ref instead of a mismatched `HTMLParagraphElement` cast.
3. specs/051-text-primitive/quickstart.md — replaced `pnpm --filter @pathableai/react test` with the actual script name `test:unit` (two occurrences).